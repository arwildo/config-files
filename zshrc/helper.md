# Remove Green Screen Davinci Resolve
delta keyer

# Zip semua di directory ini
zip -r SIMBunda.zip . -x "SIMBunda.zip"

# Zip satu file
zip khanza.zip khanza.jar

# Cari yang namanya ini di folder ini
find ./* -iname "*autosubs*"

# Transfer file antar PC via internet
Pengirim
croc send namafile.zip (Keluar kode-unik)
Penerima
croc kode-unik

# Git Reset Hard
git reset --hard a1b2c3d4
git push --force origin main

# Vim ganti kata1 menjadi kata2 yang lagi diselect
1. Select kode
2. Tekan : otomatis muncul '<,'>
3. Masukan s/kata1/kata2/g

# Cari kata berikut diseluruh file difolder ini dan sub folder
fd -t f -x grep -nH "KATA YANG DICARI" {}
